description     "supervisord-cloudml"

start on runlevel [2345]
stop on runlevel [!2345]

respawn

exec /usr/local/bin/supervisord --nodaemon --configuration {{ active_etc_link}}/supervisor/supervisord.conf
